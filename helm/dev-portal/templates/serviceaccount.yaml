{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dev-portal.serviceAccountName" . }}
  labels:
    {{- include "dev-portal.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The portal does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
