# No managed hosts. Every task runs on the controller and reaches the cluster
# through its API, so the inventory's only job is to carry the variables in
# group_vars/ for this environment.
all:
  hosts:
    controller:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
